# Paytm Sense Frontend

React/Vite frontend for the existing Paytm Sense FastAPI backend.

```powershell
cd frontend
npm.cmd install
npm.cmd run dev
```

By default the frontend uses `http://localhost:8000`. Override it with:

```powershell
VITE_API_BASE_URL=http://localhost:8000
```
